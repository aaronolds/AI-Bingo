# bingo Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-20

## Active Technologies

- .NET SDK 10 (all projects `net10.0`), C# `latest` + .NET Aspire (AppHost + service defaults), central package management (`Directory.Packages.props`) (001-repo-foundation-aspire)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for .NET SDK 10 (all projects `net10.0`), C# `latest`

## Code Style

.NET SDK 10 (all projects `net10.0`), C# `latest`: Follow standard conventions

## Recent Changes

- 001-repo-foundation-aspire: Added .NET SDK 10 (all projects `net10.0`), C# `latest` + .NET Aspire (AppHost + service defaults), central package management (`Directory.Packages.props`)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
